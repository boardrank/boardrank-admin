import styled from 'styled-components';

const Breadcrumbs = () => {
  return <StyledWrapper>Breadcrumbs</StyledWrapper>;
};

const StyledWrapper = styled.div`
  flex: 1;
`;

export default Breadcrumbs;
